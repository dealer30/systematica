import { authapi } from '../../Services/api';
import './index.css';
import SearchContainer from './SearchContainer';

function Home () {
    return (
        <SearchContainer />
    )
}

export default Home
